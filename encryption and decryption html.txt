<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title> Encryption & Decryption</title>
  <style>
    body { font-family: Arial, sans-serif; padding: 20px; }
    input, textarea, button { margin: 10px 0; width: 100%; padding: 8px; }
    button { cursor: pointer; border-radius: 8px; border: none; background: #4CAF50; color: white; }
  </style>
</head>
<body>
  <h2>Encryption and Decryption </h2>
  <h3> By Hari kumar<h3>

  <label for="message">Enter encryption message:</label>
  <textarea id="message" rows="3"></textarea>

  <label for="key">Enter Key (number):</label>
  <input type="number" id="key" >

  <button onclick="encrypt()">Encrypt</button>
  <button onclick="decrypt()">Decrypt</button>

  <h3>Result:</h3>
  <p id="result"></p>

  <script>
    function encrypt() {
      let text = document.getElementById("message").value;
      let key = parseInt(document.getElementById("key").value);
      let result = "";

      for (let i = 0; i < text.length; i++) {
        let char = text.charCodeAt(i);

        // Uppercase letters
        if (char >= 65 && char <= 90) {
          result += String.fromCharCode((char - 65 + key) % 26 + 65);
        }
        // Lowercase letters
        else if (char >= 97 && char <= 122) {
          result += String.fromCharCode((char - 97 + key) % 26 + 97);
        }
        // Non-alphabetic characters
        else {
          result += text[i];
        }
      }
      document.getElementById("result").innerText = result;
    }

    function decrypt() {
      let text = document.getElementById("message").value;
      let key = parseInt(document.getElementById("key").value);
      let result = "";

      for (let i = 0; i < text.length; i++) {
        let char = text.charCodeAt(i);

        // Uppercase letters
        if (char >= 65 && char <= 90) {
          result += String.fromCharCode((char - 65 - key + 26) % 26 + 65);
        }
        // Lowercase letters
        else if (char >= 97 && char <= 122) {
          result += String.fromCharCode((char - 97 - key + 26) % 26 + 97);
        }
        // Non-alphabetic characters
        else {
          result += text[i];
        }
      }
      document.getElementById("result").innerText = result;
    }
  </script>
</body>
</html>